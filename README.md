# different-ways-server

## Requirements
Requires a running instance of mongodb 

## Configuration
Copy ```config/environment-example.js``` to ```config/environment.js``` for a quick start

## Build Setup

``` bash
# install dependencies
$ npm install # Or yarn install

# start
$ npm start
```